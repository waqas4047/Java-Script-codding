var x=22;
var z;
var b;
// if the condition is true than the first part will be print else the second 
// part will be print which is left from colon;
(x>20)? z="true":z="false"; // we here asign value to z we can see ;
console.log(z);
// we can also write like below
b=(x==30)?"true":"false"; // we here asign value to z we can see ;
console.log(b);
b="the value is " + (x==30?"true":"false"); // we here asign value to z we can see ;
console.log(b);