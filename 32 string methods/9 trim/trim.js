var str="    hi iam not    good today     ";
console.log(str);
var b=str.trim();//trim function will remove the extra spaces from the string which is in the end or start of the string;
console.log(b);