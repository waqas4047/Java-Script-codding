setInterval(anim,1000);//the first parameter is funciton name and the second is mili second
//so it mean that this function will call in every one second after
function anim(){
    document.write("hello word <br>");
}