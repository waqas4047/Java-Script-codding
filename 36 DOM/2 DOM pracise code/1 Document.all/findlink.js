var element;
element=document.links;// it will find all the links or anchor tags in the page
console.log(element);
element=document.links[1];// it will find all the links or anchor tags in the index 1 in the page
//if not work this code so write this in the page console area
console.log(element);