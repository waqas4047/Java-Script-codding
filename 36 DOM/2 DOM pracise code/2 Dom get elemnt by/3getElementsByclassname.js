var element;
element=document.getElementsByClassName("pragraph");//this will target class of name pragraph from the page 
//if code not word run this in the console of the page
console.log(element);
element=document.getElementsByClassName("footer");//this will target clss of name footer;
console.log(element);
//if there are more class with same name then we can write the syntex like bellow
element=document.getElementsByClassName("pragraph")[1];//if code not work try in console this will target
//the call with name pragraph which is index ones
console.log(element);
