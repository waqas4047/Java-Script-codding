var element;
element=document.getElementById("top");//this will select id of name top from the page 
//if code not word run this in the console of the page
console.log(element);
element=document.getElementById("header");
console.log(element);
