var element;
//i want to see the inner text as well as the tags used inside top id so we then use >innerHTML
element=document.getElementById("top").innerHTML;
//if i want to acce the element by call then i will need to run the loop
console.log(element);