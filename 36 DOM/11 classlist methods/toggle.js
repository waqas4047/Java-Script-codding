document.getElementById("top").addEventListener("click",abc);
function abc(){
    //when i click on id top then it will add branch class to it and with css the brach class text is yellow
    document.getElementById("top").classList.toggle("branch");
};