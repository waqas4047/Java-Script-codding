//if we want to see that wether the given class exist in the tag or etc so we can write
var a;
a=document.getElementById("top").classList.contains("new");//if this new class present in the id top then
//it will return true; else false
console.log(a);
//run in togglewebsite.html