function waqas(fname="laal ",lname="singh") { //if some one bymistake not pass value to parameter so we have set the default value laas and singh
    // and if some one pass value then it will override the default value
    console.log("hello Mr "+ fname + lname);
}
waqas();
// ******************************************************************
function waqas(fname="laal ",lname="singh") { //if some one bymistake not pass value to parameter so we have set the default value laas and singh
    // and if some one pass value then it will override the default value
    console.log("hello Mr "+ fname + lname);
}
waqas("waqas ","khan");